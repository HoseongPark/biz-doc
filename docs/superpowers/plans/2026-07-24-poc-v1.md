# biz-doc POC(v1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DDD 바운디드 컨텍스트/도메인 명세(YAML)를 시각적으로 편집하는 Tauri 데스크톱 앱의 v1(POC)을 리포 루트에 구축한다.

**Architecture:** React + Vite + TypeScript 프론트엔드를 Tauri 2 셸에 담는다. Rust는 셸만 — 파일 접근은 `@tauri-apps/plugin-fs`/`plugin-dialog`를 TS에서 호출. YAML은 `yaml` 패키지의 Document API로 파싱·뮤테이션해 키 순서/스타일을 보존하고, 파일이 진실의 원천(캐시 없음, 수정 즉시 저장)이다. 다이어그램은 React Flow(`@xyflow/react`), 노드 좌표는 워크스페이스 `.config/layout.json`에 분리 저장.

**Tech Stack:** Tauri 2, React 18, Vite 5, TypeScript 5, @xyflow/react 12, yaml 2, zod 3, zustand 4, lucide-react, vitest, @fontsource/noto-sans-kr, @fontsource/jetbrains-mono

## Global Constraints

- **파일이 진실의 원천**: 워크스페이스 `context/*.yml` 직접 읽기/쓰기, 컨텍스트 1개 = 파일 1개, 캐시 없음
- **YAML 키 순서 보존 필수**: 수정하지 않은 부분은 바이트 동일하게 유지 (yaml Document API 사용, JS 객체 재직렬화 금지)
- 도메인 유형 5종: `Root Aggregate`(#4F63F6) / `Entity`(#0E9F6E) / `Value`(#8B5CF6) / `Stereotype`(#D97706) / `Service`(#0891B2)
- 용어: `business-logic` (business-rules 아님) · Stereotype은 `attributes` 대신 `values` · Service는 `attributes` 대신 `operations`(name/description/related-domains)
- `meta.name`은 한글 단순 문자열 (ko/en 맵 금지), 영문 식별자는 도메인 키
- `identity.id`(UUID)·`audit`(author/created-at/updated-at)은 앱이 자동 관리, UI에서 ID는 표시하지 않고 audit은 상세 패널 최하단 읽기 전용
- 날짜 형식: `YYYY-MM-DD HH:mm:ss`
- 노드 좌표 등 뷰 설정은 워크스페이스 `.config/layout.json` (명세 YAML에 넣지 않음)
- Rust 코드는 Tauri 초기 생성 상태 유지 (로직 추가 금지)
- 폰트: Noto Sans KR(기본) + JetBrains Mono(도메인 키·속성명·타입·로직명·열거 상수)
- 디자인 토큰은 `document/design/ui.md`의 표 값을 그대로 사용
- 개발용 워크스페이스: 리포의 `document/sample` (샘플 명세 `context/swing-session.yml`)
- 커밋 메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 추가

---

## File Structure

```
biz-doc/  (리포 루트)
├── package.json / vite.config.ts / tsconfig.json / index.html
├── src/
│   ├── main.tsx                      # React 엔트리 + 폰트/토큰 import
│   ├── App.tsx                       # 셸 레이아웃 (TopBar/Sidebar/Canvas/DetailPanel)
│   ├── styles/tokens.css             # 디자인 토큰 CSS 변수
│   ├── styles/app.css                # 레이아웃/공통 스타일
│   ├── workspace/
│   │   ├── schema.ts                 # 명세 타입 + zod 검증 (Task 3)
│   │   ├── yamlStore.ts              # yaml Document 파싱/뮤테이션/직렬화 (Task 4)
│   │   ├── fs.ts                     # FsAdapter 인터페이스 + InMemoryFs (Task 5)
│   │   ├── tauriFs.ts                # Tauri plugin-fs 구현체 (Task 5)
│   │   ├── workspaceService.ts       # 워크스페이스/레이아웃/최근경로 IO (Task 5)
│   │   └── useWorkspace.ts           # zustand 스토어 + 편집 액션 (Task 6)
│   ├── components/
│   │   ├── TopBar.tsx                # 로고/경로/새로고침/컨텍스트 추가 (Task 7)
│   │   ├── EmptyState.tsx            # 폴더 열기 + 최근 워크스페이스 (Task 7)
│   │   ├── Sidebar.tsx               # 탐색기 트리 (모델/서비스 그룹) (Task 8)
│   │   ├── ContextMap.tsx            # React Flow 캔버스 (Task 9)
│   │   ├── nodes/ContextGroupNode.tsx# 컨텍스트 박스 (모델/서비스 구역) (Task 9,10)
│   │   ├── nodes/DomainNode.tsx      # 도메인 카드 5종 (Task 10)
│   │   └── panels/DetailPanel.tsx    # 상세 편집 패널 (Task 12)
│   └── setupTests.ts
├── src-tauri/                        # Tauri 셸 (Task 2, 생성 상태 유지)
└── docs/superpowers/plans/           # 본 계획
```

테스트는 대상 파일 옆 `__tests__/` 디렉터리에 콜로케이션 (`src/workspace/__tests__/schema.test.ts` 등).

---

### Task 1: Vite + React + TS 스캐폴딩 + vitest

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.node.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `src/vite-env.d.ts`, `.gitignore`(수정)
- Test: `src/__tests__/sanity.test.ts`

**Interfaces:**
- Produces: `npm run dev`(Vite), `npm test`(vitest run), `npm run build` 동작하는 프로젝트 뼈대. 이후 모든 태스크가 이 위에 쌓임.

- [ ] **Step 1: package.json 작성**

리포 루트에 생성 (기존 `document/`, `CLAUDE.md` 등과 공존):

```json
{
  "name": "biz-doc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri"
  },
  "dependencies": {
    "@fontsource/jetbrains-mono": "^5.0.0",
    "@fontsource/noto-sans-kr": "^5.0.0",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@xyflow/react": "^12.0.0",
    "lucide-react": "^0.400.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "yaml": "^2.4.0",
    "zod": "^3.23.0",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
```

- [ ] **Step 2: vite/tsconfig/index.html/엔트리 작성**

`vite.config.ts`:

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  clearScreen: false,
  server: { port: 5173, strictPort: true },
  test: { environment: "node", include: ["src/**/*.test.ts", "src/**/*.test.tsx"] },
});
```

vitest 설정을 vite.config에 두므로 파일 상단에 `/// <reference types="vitest/config" />` 를 추가한다.

`tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2021",
    "useDefineForClassFields": true,
    "lib": ["ES2021", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["vitest/globals"]
  },
  "include": ["src"]
}
```

(vitest globals를 쓰므로 vite.config `test`에 `globals: true` 추가.)

`index.html`:

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>biz-doc studio</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/main.tsx`:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

`src/App.tsx` (임시):

```tsx
export default function App() {
  return <div>biz-doc studio</div>;
}
```

`src/vite-env.d.ts`:

```ts
/// <reference types="vite/client" />
```

`.gitignore`에 추가:

```
node_modules
dist
src-tauri/target
```

- [ ] **Step 3: 실패하는 sanity 테스트 작성**

`src/__tests__/sanity.test.ts`:

```ts
import { describe, it, expect } from "vitest";

describe("toolchain", () => {
  it("runs typescript tests", () => {
    const sum = (a: number, b: number) => a + b;
    expect(sum(1, 2)).toBe(3);
  });
});
```

- [ ] **Step 4: 설치 및 테스트 실행**

Run: `npm install` 후 `npm test`
Expected: PASS (1 test). `npm run build`도 에러 없이 통과.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json vite.config.ts tsconfig.json index.html src .gitignore
git commit -m "chore: Vite+React+TS 스캐폴딩 및 vitest 구성"
```

---

### Task 2: Tauri 2 셸 + fs/dialog 플러그인

**Files:**
- Create: `src-tauri/` 전체 (tauri init 생성물), `src-tauri/capabilities/default.json`(수정)
- Modify: `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, `src-tauri/src/lib.rs`

**Interfaces:**
- Produces: `npm run tauri dev`로 데스크톱 창 실행. 프론트에서 `@tauri-apps/plugin-fs`(임의 경로 읽기/쓰기), `@tauri-apps/plugin-dialog`(폴더 선택) 호출 가능.

- [ ] **Step 1: Tauri 초기화**

Run:
```bash
npx tauri init --app-name biz-doc --window-title "biz-doc studio" --frontend-dist ../dist --dev-url http://localhost:5173 --before-dev-command "npm run dev" --before-build-command "npm run build" --ci
```
Expected: `src-tauri/` 생성.

- [ ] **Step 2: 플러그인 추가**

Run (src-tauri에서):
```bash
cargo add tauri-plugin-fs tauri-plugin-dialog
```

`src-tauri/src/lib.rs`의 builder에 플러그인 등록 (생성된 run() 함수 수정 — Rust 로직 추가는 이 두 줄이 전부):

```rust
tauri::Builder::default()
    .plugin(tauri_plugin_fs::init())
    .plugin(tauri_plugin_dialog::init())
```

- [ ] **Step 3: capabilities 권한 설정**

`src-tauri/capabilities/default.json`의 permissions에 추가 (워크스페이스는 사용자가 고른 임의 폴더이므로 전체 스코프 허용 — POC 한정):

```json
{
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    { "identifier": "fs:allow-read-text-file", "allow": [{ "path": "**" }] },
    { "identifier": "fs:allow-write-text-file", "allow": [{ "path": "**" }] },
    { "identifier": "fs:allow-read-dir", "allow": [{ "path": "**" }] },
    { "identifier": "fs:allow-mkdir", "allow": [{ "path": "**" }] },
    { "identifier": "fs:allow-exists", "allow": [{ "path": "**" }] },
    { "identifier": "fs:allow-remove", "allow": [{ "path": "**" }] }
  ]
}
```

- [ ] **Step 4: 실행 확인**

Run: `npm run tauri dev`
Expected: "biz-doc studio" 창이 뜨고 `biz-doc studio` 텍스트 렌더. 콘솔 에러 없음. 확인 후 종료.

- [ ] **Step 5: Commit**

```bash
git add src-tauri package.json package-lock.json
git commit -m "chore: Tauri 2 셸 및 fs/dialog 플러그인 구성"
```

---

### Task 3: 명세 스키마 (schema.ts)

**Files:**
- Create: `src/workspace/schema.ts`
- Test: `src/workspace/__tests__/schema.test.ts`, `src/workspace/__tests__/fixtures/swing-session.yml` (리포의 `document/sample/context/swing-session.yml`을 복사)

**Interfaces:**
- Produces:
  - 타입: `DomainType`, `Audit`, `Attribute`, `BusinessLogic`, `EnumValue`, `Operation`, `DomainSpec`, `RelationshipEnd`, `Relationship`, `ContextSpec`
  - 상수: `DOMAIN_TYPES: readonly DomainType[]`
  - 함수: `validateContext(data: unknown): { ok: true; spec: ContextSpec } | { ok: false; errors: string[] }`
  - 함수: `nowStamp(): string` — `YYYY-MM-DD HH:mm:ss`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/workspace/__tests__/schema.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { parse } from "yaml";
import { validateContext, nowStamp, DOMAIN_TYPES } from "../schema";

const sample = parse(
  readFileSync(join(__dirname, "fixtures", "swing-session.yml"), "utf-8")
);

describe("validateContext", () => {
  it("accepts the sample spec", () => {
    const r = validateContext(sample);
    expect(r.ok).toBe(true);
    if (r.ok) {
      expect(r.spec.info.context.name).toBe("스윙 레코더 (Swing Recorder)");
      expect(Object.keys(r.spec.domain!)).toContain("SwingEvaluator");
      expect(r.spec.domain!["SwingEvaluator"].meta.identity.type).toBe("Service");
      expect(r.spec.domain!["SwingEvaluator"].operations![0]["related-domains"])
        .toEqual(["SwingSession", "SwingResult"]);
      expect(r.spec.relationships).toHaveLength(2);
    }
  });

  it("rejects unknown domain type", () => {
    const bad = structuredClone(sample);
    bad.domain.SwingSession.meta.identity.type = "Aggregate";
    const r = validateContext(bad);
    expect(r.ok).toBe(false);
    if (!r.ok) expect(r.errors.join()).toContain("SwingSession");
  });

  it("rejects missing info.context.id", () => {
    const bad = structuredClone(sample);
    delete bad.info.context.id;
    expect(validateContext(bad).ok).toBe(false);
  });
});

describe("nowStamp", () => {
  it("formats as YYYY-MM-DD HH:mm:ss", () => {
    expect(nowStamp()).toMatch(/^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$/);
  });
});

describe("DOMAIN_TYPES", () => {
  it("has the five agreed types", () => {
    expect(DOMAIN_TYPES).toEqual([
      "Root Aggregate", "Entity", "Value", "Stereotype", "Service",
    ]);
  });
});
```

fixture 준비: `document/sample/context/swing-session.yml`을 `src/workspace/__tests__/fixtures/swing-session.yml`로 복사.

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module '../schema'`

- [ ] **Step 3: 구현**

`src/workspace/schema.ts`:

```ts
import { z } from "zod";

export const DOMAIN_TYPES = [
  "Root Aggregate",
  "Entity",
  "Value",
  "Stereotype",
  "Service",
] as const;
export type DomainType = (typeof DOMAIN_TYPES)[number];

const stampRe = /^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$/;

const auditSchema = z.object({
  author: z.string(),
  "created-at": z.string().regex(stampRe),
  "updated-at": z.string().regex(stampRe),
});

const attributeSchema = z.object({
  name: z.string().min(1),
  type: z.string().min(1),
  description: z.string().optional(),
});

const namedSchema = z.object({
  name: z.string().min(1),
  description: z.string().optional(),
});

const operationSchema = namedSchema.extend({
  "related-domains": z.array(z.string()).optional(),
});

const domainSchema = z.object({
  meta: z.object({
    identity: z.object({
      id: z.string().uuid(),
      type: z.enum(DOMAIN_TYPES),
    }),
    name: z.string().min(1),
    description: z.string().optional(),
    audit: auditSchema,
  }),
  attributes: z.array(attributeSchema).optional(),
  "business-logic": z.array(namedSchema).optional(),
  values: z.array(namedSchema).optional(),
  operations: z.array(operationSchema).optional(),
});

const relationshipEndSchema = z.object({
  "context-id": z.string().uuid(),
  "domain-id": z.string().uuid(),
});

const relationshipSchema = z.object({
  from: relationshipEndSchema,
  to: relationshipEndSchema,
  relationship: z.string().min(1),
});

export const contextSchema = z.object({
  info: z.object({
    context: z.object({ id: z.string().uuid(), name: z.string().min(1) }),
    audit: auditSchema,
  }),
  domain: z.record(domainSchema).optional(),
  relationships: z.array(relationshipSchema).optional(),
});

export type Audit = z.infer<typeof auditSchema>;
export type Attribute = z.infer<typeof attributeSchema>;
export type BusinessLogic = z.infer<typeof namedSchema>;
export type EnumValue = z.infer<typeof namedSchema>;
export type Operation = z.infer<typeof operationSchema>;
export type DomainSpec = z.infer<typeof domainSchema>;
export type RelationshipEnd = z.infer<typeof relationshipEndSchema>;
export type Relationship = z.infer<typeof relationshipSchema>;
export type ContextSpec = z.infer<typeof contextSchema>;

export function validateContext(
  data: unknown
): { ok: true; spec: ContextSpec } | { ok: false; errors: string[] } {
  const r = contextSchema.safeParse(data);
  if (r.success) return { ok: true, spec: r.data };
  return {
    ok: false,
    errors: r.error.issues.map((i) => `${i.path.join(".")}: ${i.message}`),
  };
}

export function nowStamp(d = new Date()): string {
  const p = (n: number) => String(n).padStart(2, "0");
  return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())} ${p(
    d.getHours()
  )}:${p(d.getMinutes())}:${p(d.getSeconds())}`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS (schema 5 tests + sanity)

- [ ] **Step 5: Commit**

```bash
git add src/workspace/schema.ts src/workspace/__tests__
git commit -m "feat: 명세 스키마 타입 및 zod 검증"
```

---

### Task 4: YAML 저장소 (yamlStore.ts) — 키 순서 보존

**Files:**
- Create: `src/workspace/yamlStore.ts`
- Test: `src/workspace/__tests__/yamlStore.test.ts` (Task 3의 fixture 재사용)

**Interfaces:**
- Consumes: `validateContext`, `ContextSpec` (Task 3)
- Produces:
  - `parseContext(text: string): { doc: Document; spec: ContextSpec }` — 실패 시 `Error(errors.join("\n"))` throw
  - `serializeContext(doc: Document): string`
  - `setDomainValue(doc: Document, domainKey: string, path: (string | number)[], value: unknown): void` — `domain.<key>.<path>`에 값 설정 + `meta.audit.updated-at` 갱신
  - `deleteDomainValue(doc, domainKey, path)` — 값 제거 + audit 갱신
  - `addDomain(doc, domainKey, domain: DomainSpec): void`
  - `removeDomain(doc, domainKey): void`
  - `setRelationships(doc, rels: Relationship[]): void`
  - `newContextText(name: string, author: string): string` — 새 컨텍스트 yml 원문 생성

- [ ] **Step 1: 실패하는 테스트 작성**

`src/workspace/__tests__/yamlStore.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";
import {
  parseContext, serializeContext, setDomainValue, addDomain,
  removeDomain, setRelationships, newContextText,
} from "../yamlStore";
import { validateContext } from "../schema";

const text = readFileSync(
  join(__dirname, "fixtures", "swing-session.yml"), "utf-8"
);

describe("round-trip", () => {
  it("re-serializes untouched document identically", () => {
    const { doc } = parseContext(text);
    expect(serializeContext(doc)).toBe(text);
  });
});

describe("setDomainValue", () => {
  it("updates a nested value and audit, preserving key order", () => {
    const { doc } = parseContext(text);
    setDomainValue(doc, "SwingSession", ["attributes", 0, "description"], "시작 시각");
    const out = serializeContext(doc);
    const { spec } = parseContext(out);
    expect(spec.domain!["SwingSession"].attributes![0].description).toBe("시작 시각");
    expect(spec.domain!["SwingSession"].meta.audit["updated-at"]).not.toBe(
      "2026-07-23 10:46:00"
    );
    const keys = out.match(/^  \w+:/gm)!.map((s) => s.trim());
    expect(keys).toEqual([
      "SwingSession:", "SwingRecorder:", "SwingInterval:",
      "SwingResult:", "SwingEvaluator:",
    ]);
  });
});

describe("addDomain / removeDomain", () => {
  it("appends a new domain at the end and removes it", () => {
    const { doc } = parseContext(text);
    addDomain(doc, "NewThing", {
      meta: {
        identity: { id: "0d4f7a1e-9b2c-4d3e-8f5a-6b7c8d9e0f1a", type: "Value" },
        name: "새 값",
        description: "테스트",
        audit: {
          author: "박호성",
          "created-at": "2026-07-24 12:00:00",
          "updated-at": "2026-07-24 12:00:00",
        },
      },
      attributes: [],
    });
    let spec = parseContext(serializeContext(doc)).spec;
    expect(Object.keys(spec.domain!).at(-1)).toBe("NewThing");
    removeDomain(doc, "NewThing");
    spec = parseContext(serializeContext(doc)).spec;
    expect(spec.domain!["NewThing"]).toBeUndefined();
  });
});

describe("setRelationships", () => {
  it("replaces the relationships list", () => {
    const { doc, spec } = parseContext(text);
    const rels = spec.relationships!.slice(0, 1);
    setRelationships(doc, rels);
    expect(parseContext(serializeContext(doc)).spec.relationships).toHaveLength(1);
  });
});

describe("newContextText", () => {
  it("creates a valid empty context", () => {
    const t = newContextText("주문", "박호성");
    const { spec } = parseContext(t);
    expect(spec.info.context.name).toBe("주문");
    expect(validateContext(spec).ok).toBe(true);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module '../yamlStore'`

- [ ] **Step 3: 구현**

`src/workspace/yamlStore.ts`:

```ts
import { Document, parseDocument } from "yaml";
import {
  ContextSpec, DomainSpec, Relationship, validateContext, nowStamp,
} from "./schema";

export function parseContext(text: string): { doc: Document; spec: ContextSpec } {
  const doc = parseDocument(text);
  if (doc.errors.length) {
    throw new Error(doc.errors.map((e) => e.message).join("\n"));
  }
  const r = validateContext(doc.toJS());
  if (!r.ok) throw new Error(r.errors.join("\n"));
  return { doc, spec: r.spec };
}

export function serializeContext(doc: Document): string {
  return doc.toString();
}

function touchAudit(doc: Document, domainKey: string): void {
  doc.setIn(["domain", domainKey, "meta", "audit", "updated-at"], nowStamp());
}

export function setDomainValue(
  doc: Document, domainKey: string,
  path: (string | number)[], value: unknown
): void {
  doc.setIn(["domain", domainKey, ...path], value);
  touchAudit(doc, domainKey);
}

export function deleteDomainValue(
  doc: Document, domainKey: string, path: (string | number)[]
): void {
  doc.deleteIn(["domain", domainKey, ...path]);
  touchAudit(doc, domainKey);
}

export function addDomain(doc: Document, domainKey: string, domain: DomainSpec): void {
  doc.setIn(["domain", domainKey], doc.createNode(domain));
}

export function removeDomain(doc: Document, domainKey: string): void {
  doc.deleteIn(["domain", domainKey]);
}

export function setRelationships(doc: Document, rels: Relationship[]): void {
  if (rels.length === 0) doc.setIn(["relationships"], doc.createNode([]));
  else doc.setIn(["relationships"], doc.createNode(rels));
}

export function newContextText(name: string, author: string): string {
  const stamp = nowStamp();
  const doc = new Document({
    info: {
      context: { id: crypto.randomUUID(), name },
      audit: { author, "created-at": stamp, "updated-at": stamp },
    },
    domain: {},
    relationships: [],
  });
  return doc.toString();
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS. **round-trip 테스트가 실패하면** (yaml 패키지가 개행/스타일을 다르게 낼 경우) fixture와 실제 출력을 diff해 원인을 확인하고, `serializeContext`에 `doc.toString({ lineWidth: 0 })` 등 옵션을 조정해 반드시 바이트 동일성을 만족시킨 뒤 진행한다. 이 보장은 프로젝트의 핵심 요구사항이다.

- [ ] **Step 5: Commit**

```bash
git add src/workspace/yamlStore.ts src/workspace/__tests__/yamlStore.test.ts
git commit -m "feat: 키 순서 보존 YAML 저장소"
```

---

### Task 5: 파일 IO 추상화 + 워크스페이스 서비스

**Files:**
- Create: `src/workspace/fs.ts`, `src/workspace/tauriFs.ts`, `src/workspace/workspaceService.ts`
- Test: `src/workspace/__tests__/workspaceService.test.ts`

**Interfaces:**
- Consumes: `parseContext` (Task 4)
- Produces:
  - `interface FsAdapter { readTextFile(path): Promise<string>; writeTextFile(path, text): Promise<void>; readDir(path): Promise<string[]>; exists(path): Promise<boolean>; mkdir(path): Promise<void>; remove(path): Promise<void> }`
  - `class InMemoryFs implements FsAdapter` — 생성자에 `Record<path, string>` 시드
  - `tauriFs: FsAdapter` (plugin-fs 래핑; 테스트에서 import 안 함)
  - `joinPath(...parts: string[]): string` — `/` 결합
  - `interface ContextFile { fileName: string; doc: Document; spec: ContextSpec; error?: string }`
  - `interface LayoutFile { nodes: Record<string, { x: number; y: number }> }`
  - `loadWorkspace(fs, root): Promise<{ contexts: ContextFile[]; layout: LayoutFile }>` — `context/*.yml` 전부 로드, 파싱 실패 파일은 `error`만 채워 반환
  - `saveContextFile(fs, root, file: ContextFile): Promise<void>`
  - `saveLayout(fs, root, layout): Promise<void>` — `.config/` 없으면 생성
  - `loadRecent(fs, configDir): Promise<string[]>` / `saveRecent(fs, configDir, paths): Promise<void>` — `recent.json`, 최대 5개

- [ ] **Step 1: 실패하는 테스트 작성**

`src/workspace/__tests__/workspaceService.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { InMemoryFs } from "../fs";
import {
  loadWorkspace, saveContextFile, saveLayout, loadRecent, saveRecent,
} from "../workspaceService";
import { setDomainValue } from "../yamlStore";

const sample = readFileSync(
  join(__dirname, "fixtures", "swing-session.yml"), "utf-8"
);

function seedFs() {
  return new InMemoryFs({
    "/ws/context/swing-session.yml": sample,
    "/ws/context/broken.yml": "info: [unclosed",
    "/ws/.config/layout.json": JSON.stringify({ nodes: { abc: { x: 1, y: 2 } } }),
  });
}

describe("loadWorkspace", () => {
  it("loads valid contexts and reports broken ones", async () => {
    const { contexts, layout } = await loadWorkspace(seedFs(), "/ws");
    expect(contexts).toHaveLength(2);
    const ok = contexts.find((c) => c.fileName === "swing-session.yml")!;
    expect(ok.spec.info.context.name).toContain("스윙 레코더");
    const broken = contexts.find((c) => c.fileName === "broken.yml")!;
    expect(broken.error).toBeTruthy();
    expect(layout.nodes["abc"]).toEqual({ x: 1, y: 2 });
  });

  it("returns empty layout when .config/layout.json is missing", async () => {
    const fs = new InMemoryFs({ "/ws/context/swing-session.yml": sample });
    const { layout } = await loadWorkspace(fs, "/ws");
    expect(layout.nodes).toEqual({});
  });
});

describe("saveContextFile", () => {
  it("writes the mutated document back to disk", async () => {
    const fs = seedFs();
    const { contexts } = await loadWorkspace(fs, "/ws");
    const file = contexts.find((c) => c.fileName === "swing-session.yml")!;
    setDomainValue(file.doc, "SwingSession", ["meta", "description"], "변경됨");
    await saveContextFile(fs, "/ws", file);
    const written = await fs.readTextFile("/ws/context/swing-session.yml");
    expect(written).toContain("변경됨");
  });
});

describe("saveLayout / recent", () => {
  it("creates .config and persists layout", async () => {
    const fs = new InMemoryFs({ "/ws/context/swing-session.yml": sample });
    await saveLayout(fs, "/ws", { nodes: { n1: { x: 10, y: 20 } } });
    expect(JSON.parse(await fs.readTextFile("/ws/.config/layout.json")).nodes.n1)
      .toEqual({ x: 10, y: 20 });
  });

  it("keeps at most 5 recent paths, most recent first, deduped", async () => {
    const fs = new InMemoryFs({});
    await saveRecent(fs, "/cfg", ["a", "b", "c", "d", "e", "f"]);
    expect(await loadRecent(fs, "/cfg")).toEqual(["a", "b", "c", "d", "e"]);
    await saveRecent(fs, "/cfg", ["b", "a", "b"]);
    expect(await loadRecent(fs, "/cfg")).toEqual(["b", "a"]);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module '../fs'`

- [ ] **Step 3: 구현**

`src/workspace/fs.ts`:

```ts
export interface FsAdapter {
  readTextFile(path: string): Promise<string>;
  writeTextFile(path: string, text: string): Promise<void>;
  readDir(path: string): Promise<string[]>;
  exists(path: string): Promise<boolean>;
  mkdir(path: string): Promise<void>;
  remove(path: string): Promise<void>;
}

export function joinPath(...parts: string[]): string {
  return parts
    .map((p, i) => (i === 0 ? p.replace(/[/\\]+$/, "") : p.replace(/^[/\\]+|[/\\]+$/g, "")))
    .join("/");
}

export class InMemoryFs implements FsAdapter {
  constructor(private files: Record<string, string> = {}) {}

  async readTextFile(path: string): Promise<string> {
    if (!(path in this.files)) throw new Error(`ENOENT: ${path}`);
    return this.files[path];
  }
  async writeTextFile(path: string, text: string): Promise<void> {
    this.files[path] = text;
  }
  async readDir(path: string): Promise<string[]> {
    const prefix = path.replace(/[/\\]+$/, "") + "/";
    const names = new Set<string>();
    for (const key of Object.keys(this.files)) {
      if (key.startsWith(prefix)) names.add(key.slice(prefix.length).split("/")[0]);
    }
    return [...names];
  }
  async exists(path: string): Promise<boolean> {
    const prefix = path.replace(/[/\\]+$/, "") + "/";
    return path in this.files || Object.keys(this.files).some((k) => k.startsWith(prefix));
  }
  async mkdir(): Promise<void> {}
  async remove(path: string): Promise<void> {
    delete this.files[path];
  }
}
```

`src/workspace/tauriFs.ts`:

```ts
import {
  readTextFile, writeTextFile, readDir, exists, mkdir, remove,
} from "@tauri-apps/plugin-fs";
import type { FsAdapter } from "./fs";

export const tauriFs: FsAdapter = {
  readTextFile: (p) => readTextFile(p),
  writeTextFile: (p, t) => writeTextFile(p, t),
  readDir: async (p) => (await readDir(p)).map((e) => e.name),
  exists: (p) => exists(p),
  mkdir: (p) => mkdir(p, { recursive: true }),
  remove: (p) => remove(p),
};
```

`src/workspace/workspaceService.ts`:

```ts
import type { Document } from "yaml";
import type { FsAdapter } from "./fs";
import { joinPath } from "./fs";
import type { ContextSpec } from "./schema";
import { parseContext, serializeContext } from "./yamlStore";

export interface ContextFile {
  fileName: string;
  doc: Document;
  spec: ContextSpec;
  error?: string;
}

export interface LayoutFile {
  nodes: Record<string, { x: number; y: number }>;
}

export async function loadWorkspace(
  fs: FsAdapter, root: string
): Promise<{ contexts: ContextFile[]; layout: LayoutFile }> {
  const contextDir = joinPath(root, "context");
  const names = (await fs.exists(contextDir)) ? await fs.readDir(contextDir) : [];
  const contexts: ContextFile[] = [];
  for (const name of names.filter((n) => n.endsWith(".yml")).sort()) {
    const text = await fs.readTextFile(joinPath(contextDir, name));
    try {
      const { doc, spec } = parseContext(text);
      contexts.push({ fileName: name, doc, spec });
    } catch (e) {
      contexts.push({
        fileName: name,
        doc: undefined as unknown as Document,
        spec: undefined as unknown as ContextSpec,
        error: e instanceof Error ? e.message : String(e),
      });
    }
  }
  let layout: LayoutFile = { nodes: {} };
  const layoutPath = joinPath(root, ".config", "layout.json");
  if (await fs.exists(layoutPath)) {
    try {
      layout = JSON.parse(await fs.readTextFile(layoutPath));
      layout.nodes ??= {};
    } catch {
      layout = { nodes: {} };
    }
  }
  return { contexts, layout };
}

export async function saveContextFile(
  fs: FsAdapter, root: string, file: ContextFile
): Promise<void> {
  await fs.writeTextFile(
    joinPath(root, "context", file.fileName),
    serializeContext(file.doc)
  );
}

export async function saveLayout(
  fs: FsAdapter, root: string, layout: LayoutFile
): Promise<void> {
  await fs.mkdir(joinPath(root, ".config"));
  await fs.writeTextFile(
    joinPath(root, ".config", "layout.json"),
    JSON.stringify(layout, null, 2)
  );
}

export async function loadRecent(fs: FsAdapter, configDir: string): Promise<string[]> {
  const p = joinPath(configDir, "recent.json");
  if (!(await fs.exists(p))) return [];
  try {
    const arr = JSON.parse(await fs.readTextFile(p));
    return Array.isArray(arr) ? arr : [];
  } catch {
    return [];
  }
}

export async function saveRecent(
  fs: FsAdapter, configDir: string, paths: string[]
): Promise<void> {
  const deduped = [...new Set(paths)].slice(0, 5);
  await fs.mkdir(configDir);
  await fs.writeTextFile(joinPath(configDir, "recent.json"), JSON.stringify(deduped));
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS (전체)

- [ ] **Step 5: Commit**

```bash
git add src/workspace/fs.ts src/workspace/tauriFs.ts src/workspace/workspaceService.ts src/workspace/__tests__/workspaceService.test.ts
git commit -m "feat: 파일 IO 추상화 및 워크스페이스 서비스"
```

---

### Task 6: 앱 상태 스토어 (useWorkspace.ts)

**Files:**
- Create: `src/workspace/useWorkspace.ts`
- Test: `src/workspace/__tests__/useWorkspace.test.ts`

**Interfaces:**
- Consumes: Task 3–5 전부
- Produces: zustand 스토어 `useWorkspace` + 순수 편집 액션. 상태/액션 시그니처:

```ts
interface Selection =
  | { kind: "domain"; fileName: string; domainKey: string }
  | { kind: "context"; fileName: string }
  | { kind: "relationship"; fileName: string; index: number }
  | null;

interface WorkspaceState {
  fs: FsAdapter;                       // 기본 tauriFs, 테스트에서 교체
  root: string | null;
  contexts: ContextFile[];
  layout: LayoutFile;
  selection: Selection;
  // actions
  openWorkspace(root: string): Promise<void>;
  refresh(): Promise<void>;
  select(sel: Selection): void;
  updateDomainMeta(fileName, domainKey, patch: { name?: string; description?: string; type?: DomainType }): Promise<void>;
  upsertAttribute(fileName, domainKey, index: number | null, attr: Attribute): Promise<void>;   // null = append
  removeAttribute(fileName, domainKey, index): Promise<void>;
  upsertLogic(fileName, domainKey, index: number | null, item: BusinessLogic): Promise<void>;
  removeLogic(fileName, domainKey, index): Promise<void>;
  upsertValue(fileName, domainKey, index: number | null, item: EnumValue): Promise<void>;
  removeValue(fileName, domainKey, index): Promise<void>;
  upsertOperation(fileName, domainKey, index: number | null, item: Operation): Promise<void>;
  removeOperation(fileName, domainKey, index): Promise<void>;
  addDomain(fileName, domainKey, type: DomainType, name: string): Promise<void>;
  deleteDomain(fileName, domainKey): Promise<void>;
  addRelationship(fileName, rel: Relationship): Promise<void>;
  updateRelationship(fileName, index, label: string): Promise<void>;
  removeRelationship(fileName, index): Promise<void>;
  addContext(fileName: string, name: string): Promise<void>;
  deleteContext(fileName: string): Promise<void>;
  saveNodePosition(nodeId: string, pos: { x: number; y: number }): Promise<void>;
}
```

모든 편집 액션의 공통 흐름: **doc 뮤테이션 → saveContextFile(즉시 저장) → spec 재파생(doc.toJS + validate) → set()**. 도메인 뮤테이션은 yamlStore가 audit.updated-at을 자동 갱신. `addDomain`은 `crypto.randomUUID()`로 id, author는 해당 컨텍스트 `info.audit.author`, 유형별 기본 섹션(Stereotype→`values: []`, Service→`operations: []`, 그 외→`attributes: []`, Root Aggregate/Entity는 `business-logic: []` 추가)을 생성.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/workspace/__tests__/useWorkspace.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { readFileSync } from "node:fs";
import { join } from "node:path";
import { InMemoryFs } from "../fs";
import { useWorkspace } from "../useWorkspace";

const sample = readFileSync(
  join(__dirname, "fixtures", "swing-session.yml"), "utf-8"
);
const FILE = "swing-session.yml";

let fs: InMemoryFs;

beforeEach(async () => {
  fs = new InMemoryFs({ "/ws/context/swing-session.yml": sample });
  useWorkspace.setState({ fs, root: null, contexts: [], layout: { nodes: {} }, selection: null });
  await useWorkspace.getState().openWorkspace("/ws");
});

const spec = () =>
  useWorkspace.getState().contexts.find((c) => c.fileName === FILE)!.spec;
const written = () => fs.readTextFile("/ws/context/swing-session.yml");

describe("openWorkspace", () => {
  it("loads contexts", () => {
    expect(spec().info.context.name).toContain("스윙 레코더");
  });
});

describe("updateDomainMeta", () => {
  it("renames a domain and persists to disk", async () => {
    await useWorkspace.getState().updateDomainMeta(FILE, "SwingSession", { name: "세션" });
    expect(spec().domain!["SwingSession"].meta.name).toBe("세션");
    expect(await written()).toContain("name: 세션");
  });
});

describe("attribute CRUD", () => {
  it("appends, updates, removes", async () => {
    const s = useWorkspace.getState();
    await s.upsertAttribute(FILE, "SwingSession", null, {
      name: "place", type: "String", description: "장소",
    });
    expect(spec().domain!["SwingSession"].attributes).toHaveLength(3);
    await s.upsertAttribute(FILE, "SwingSession", 2, {
      name: "place", type: "String", description: "연습장",
    });
    expect(spec().domain!["SwingSession"].attributes![2].description).toBe("연습장");
    await s.removeAttribute(FILE, "SwingSession", 2);
    expect(spec().domain!["SwingSession"].attributes).toHaveLength(2);
  });
});

describe("addDomain", () => {
  it("creates a Service with operations section", async () => {
    await useWorkspace.getState().addDomain(FILE, "SwingSync", "Service", "스윙 동기화");
    const d = spec().domain!["SwingSync"];
    expect(d.meta.identity.type).toBe("Service");
    expect(d.operations).toEqual([]);
    expect(d.meta.identity.id).toMatch(/^[0-9a-f-]{36}$/);
    expect(d.meta.audit.author).toBe("박호성");
  });
});

describe("deleteDomain", () => {
  it("removes domain and its relationships", async () => {
    await useWorkspace.getState().deleteDomain(FILE, "SwingSession");
    expect(spec().domain!["SwingSession"]).toBeUndefined();
    expect(spec().relationships ?? []).toHaveLength(0);
  });
});

describe("relationships", () => {
  it("adds, renames, removes", async () => {
    const s = useWorkspace.getState();
    const end = (id: string) => ({
      "context-id": spec().info.context.id, "domain-id": id,
    });
    const sessionId = spec().domain!["SwingSession"].meta.identity.id;
    const resultId = spec().domain!["SwingResult"].meta.identity.id;
    await s.addRelationship(FILE, {
      from: end(sessionId), to: end(resultId), relationship: "산출함",
    });
    expect(spec().relationships).toHaveLength(3);
    await s.updateRelationship(FILE, 2, "생성함");
    expect(spec().relationships![2].relationship).toBe("생성함");
    await s.removeRelationship(FILE, 2);
    expect(spec().relationships).toHaveLength(2);
  });
});

describe("addContext / deleteContext", () => {
  it("creates and deletes a context file", async () => {
    const s = useWorkspace.getState();
    await s.addContext("order.yml", "주문");
    expect(useWorkspace.getState().contexts.map((c) => c.fileName)).toContain("order.yml");
    expect(await fs.exists("/ws/context/order.yml")).toBe(true);
    await s.deleteContext("order.yml");
    expect(await fs.exists("/ws/context/order.yml")).toBe(false);
  });
});

describe("saveNodePosition", () => {
  it("persists layout.json", async () => {
    await useWorkspace.getState().saveNodePosition("node-1", { x: 5, y: 9 });
    const layout = JSON.parse(await fs.readTextFile("/ws/.config/layout.json"));
    expect(layout.nodes["node-1"]).toEqual({ x: 5, y: 9 });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module '../useWorkspace'`

- [ ] **Step 3: 구현**

`src/workspace/useWorkspace.ts`:

```ts
import { create } from "zustand";
import type { FsAdapter } from "./fs";
import { tauriFs } from "./tauriFs";
import {
  Attribute, BusinessLogic, DomainSpec, DomainType, EnumValue, Operation,
  Relationship, nowStamp, validateContext,
} from "./schema";
import {
  addDomain as docAddDomain, deleteDomainValue, removeDomain as docRemoveDomain,
  newContextText, parseContext, setDomainValue, setRelationships,
} from "./yamlStore";
import {
  ContextFile, LayoutFile, loadWorkspace, saveContextFile, saveLayout,
} from "./workspaceService";
import { joinPath } from "./fs";

export type Selection =
  | { kind: "domain"; fileName: string; domainKey: string }
  | { kind: "context"; fileName: string }
  | { kind: "relationship"; fileName: string; index: number }
  | null;

interface WorkspaceState {
  fs: FsAdapter;
  root: string | null;
  contexts: ContextFile[];
  layout: LayoutFile;
  selection: Selection;
  openWorkspace(root: string): Promise<void>;
  refresh(): Promise<void>;
  select(sel: Selection): void;
  updateDomainMeta(
    fileName: string, domainKey: string,
    patch: { name?: string; description?: string; type?: DomainType }
  ): Promise<void>;
  upsertAttribute(f: string, d: string, i: number | null, a: Attribute): Promise<void>;
  removeAttribute(f: string, d: string, i: number): Promise<void>;
  upsertLogic(f: string, d: string, i: number | null, x: BusinessLogic): Promise<void>;
  removeLogic(f: string, d: string, i: number): Promise<void>;
  upsertValue(f: string, d: string, i: number | null, x: EnumValue): Promise<void>;
  removeValue(f: string, d: string, i: number): Promise<void>;
  upsertOperation(f: string, d: string, i: number | null, x: Operation): Promise<void>;
  removeOperation(f: string, d: string, i: number): Promise<void>;
  addDomain(f: string, key: string, type: DomainType, name: string): Promise<void>;
  deleteDomain(f: string, key: string): Promise<void>;
  addRelationship(f: string, rel: Relationship): Promise<void>;
  updateRelationship(f: string, i: number, label: string): Promise<void>;
  removeRelationship(f: string, i: number): Promise<void>;
  addContext(fileName: string, name: string): Promise<void>;
  deleteContext(fileName: string): Promise<void>;
  saveNodePosition(nodeId: string, pos: { x: number; y: number }): Promise<void>;
}

export const useWorkspace = create<WorkspaceState>((set, get) => {
  async function commit(fileName: string, mutate: (file: ContextFile) => void) {
    const { fs, root, contexts } = get();
    if (!root) throw new Error("workspace not open");
    const file = contexts.find((c) => c.fileName === fileName);
    if (!file || file.error) throw new Error(`context not loaded: ${fileName}`);
    mutate(file);
    const r = validateContext(file.doc.toJS());
    if (!r.ok) throw new Error(r.errors.join("\n"));
    file.spec = r.spec;
    await saveContextFile(fs, root, file);
    set({ contexts: [...contexts] });
  }

  function upsertListItem(section: string) {
    return (f: string, d: string, i: number | null, item: unknown) =>
      commit(f, (file) => {
        const list = (file.spec.domain![d] as Record<string, unknown[]>)[section] ?? [];
        const index = i === null ? list.length : i;
        setDomainValue(file.doc, d, [section, index], file.doc.createNode(item));
      });
  }

  function removeListItem(section: string) {
    return (f: string, d: string, i: number) =>
      commit(f, (file) => deleteDomainValue(file.doc, d, [section, i]));
  }

  return {
    fs: tauriFs,
    root: null,
    contexts: [],
    layout: { nodes: {} },
    selection: null,

    async openWorkspace(root) {
      const { contexts, layout } = await loadWorkspace(get().fs, root);
      set({ root, contexts, layout, selection: null });
    },

    async refresh() {
      const { root } = get();
      if (root) await get().openWorkspace(root);
    },

    select(selection) {
      set({ selection });
    },

    updateDomainMeta(f, d, patch) {
      return commit(f, (file) => {
        if (patch.name !== undefined) setDomainValue(file.doc, d, ["meta", "name"], patch.name);
        if (patch.description !== undefined)
          setDomainValue(file.doc, d, ["meta", "description"], patch.description);
        if (patch.type !== undefined)
          setDomainValue(file.doc, d, ["meta", "identity", "type"], patch.type);
      });
    },

    upsertAttribute: upsertListItem("attributes"),
    removeAttribute: removeListItem("attributes"),
    upsertLogic: upsertListItem("business-logic"),
    removeLogic: removeListItem("business-logic"),
    upsertValue: upsertListItem("values"),
    removeValue: removeListItem("values"),
    upsertOperation: upsertListItem("operations"),
    removeOperation: removeListItem("operations"),

    addDomain(f, key, type, name) {
      return commit(f, (file) => {
        const stamp = nowStamp();
        const domain: DomainSpec = {
          meta: {
            identity: { id: crypto.randomUUID(), type },
            name,
            description: "",
            audit: {
              author: file.spec.info.audit.author,
              "created-at": stamp,
              "updated-at": stamp,
            },
          },
          ...(type === "Stereotype"
            ? { values: [] }
            : type === "Service"
            ? { operations: [] }
            : { attributes: [], "business-logic": [] }),
        };
        docAddDomain(file.doc, key, domain);
      });
    },

    deleteDomain(f, key) {
      return commit(f, (file) => {
        const id = file.spec.domain![key].meta.identity.id;
        docRemoveDomain(file.doc, key);
        const rels = (file.spec.relationships ?? []).filter(
          (r) => r.from["domain-id"] !== id && r.to["domain-id"] !== id
        );
        setRelationships(file.doc, rels);
      });
    },

    addRelationship(f, rel) {
      return commit(f, (file) => {
        setRelationships(file.doc, [...(file.spec.relationships ?? []), rel]);
      });
    },

    updateRelationship(f, i, label) {
      return commit(f, (file) => {
        file.doc.setIn(["relationships", i, "relationship"], label);
      });
    },

    removeRelationship(f, i) {
      return commit(f, (file) => {
        const rels = [...(file.spec.relationships ?? [])];
        rels.splice(i, 1);
        setRelationships(file.doc, rels);
      });
    },

    async addContext(fileName, name) {
      const { fs, root } = get();
      if (!root) throw new Error("workspace not open");
      const author =
        get().contexts.find((c) => !c.error)?.spec.info.audit.author ?? "unknown";
      await fs.writeTextFile(
        joinPath(root, "context", fileName),
        newContextText(name, author)
      );
      await get().refresh();
    },

    async deleteContext(fileName) {
      const { fs, root } = get();
      if (!root) throw new Error("workspace not open");
      await fs.remove(joinPath(root, "context", fileName));
      await get().refresh();
    },

    async saveNodePosition(nodeId, pos) {
      const { fs, root, layout } = get();
      if (!root) return;
      const next = { nodes: { ...layout.nodes, [nodeId]: pos } };
      set({ layout: next });
      await saveLayout(fs, root, next);
    },
  };
});
```

주의: `upsertListItem`은 존재하지 않는 섹션 인덱스에 setIn하면 yaml이 자동으로 시퀀스를 만들지 않는 경우가 있다. 테스트가 실패하면 `commit` 안에서 섹션 부재 시 `file.doc.setIn(["domain", d, section], file.doc.createNode([]))`로 먼저 생성한 뒤 항목을 넣도록 보강한다.

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS (전체). vitest는 node 환경이므로 `tauriFs` import가 평가되지만 호출되지는 않음 — import 시점 에러가 나면 `tauriFs`를 lazy(함수 내 dynamic import)로 바꾼다.

- [ ] **Step 5: Commit**

```bash
git add src/workspace/useWorkspace.ts src/workspace/__tests__/useWorkspace.test.ts
git commit -m "feat: 워크스페이스 상태 스토어 및 편집 액션"
```

---

### Task 7: 디자인 토큰 + 앱 셸 + 빈 상태(폴더 열기)

**Files:**
- Create: `src/styles/tokens.css`, `src/styles/app.css`, `src/components/TopBar.tsx`, `src/components/EmptyState.tsx`
- Modify: `src/App.tsx`, `src/main.tsx`

**Interfaces:**
- Consumes: `useWorkspace` (Task 6)
- Produces: 상단바 + 3단 레이아웃(사이드바 240px / 캔버스 / 상세 300px) 골격. 워크스페이스 미선택 시 EmptyState(폴더 열기 + 최근 목록). `openFolder(): Promise<void>` 헬퍼(TopBar/EmptyState 공용, dialog.open → openWorkspace → recent 저장).

- [ ] **Step 1: 토큰/레이아웃 CSS 작성**

`src/styles/tokens.css` (ui.md 디자인 토큰 표 그대로):

```css
:root {
  --bg: #f6f7f9;
  --surface: #ffffff;
  --canvas-bg: #eef0f3;
  --border: #e2e5ea;
  --text-primary: #1f2430;
  --text-secondary: #6b7280;
  --accent: #4f63f6;
  --accent-soft: #eef0fe;
  --danger: #dc2626;
  --type-root: #4f63f6;
  --type-root-soft: #eef0fe;
  --type-entity: #0e9f6e;
  --type-entity-soft: #e7f6f0;
  --type-value: #8b5cf6;
  --type-value-soft: #f1ebfe;
  --type-stereotype: #d97706;
  --type-stereotype-soft: #fdf0df;
  --type-service: #0891b2;
  --type-service-soft: #e0f2fe;
  --font: "Noto Sans KR", sans-serif;
  --font-mono: "JetBrains Mono", monospace;
}
```

`src/styles/app.css`:

```css
* { box-sizing: border-box; margin: 0; }
html, body, #root { height: 100%; }
body { font-family: var(--font); color: var(--text-primary); background: var(--bg); font-size: 13px; }
button { font-family: inherit; cursor: pointer; }
input, select, textarea { font-family: inherit; font-size: 13px; }

.app { display: flex; flex-direction: column; height: 100%; }
.topbar {
  display: flex; align-items: center; gap: 12px; height: 52px; padding: 0 16px;
  background: var(--surface); border-bottom: 1px solid var(--border); flex: none;
}
.topbar .logo { font-weight: 700; font-size: 15px; }
.topbar .ws-pill {
  display: flex; align-items: center; gap: 6px; background: var(--bg);
  border-radius: 6px; padding: 5px 10px; color: var(--text-secondary); font-size: 12px;
}
.topbar .spacer { flex: 1; }
.btn {
  display: inline-flex; align-items: center; gap: 6px; border-radius: 6px;
  padding: 7px 12px; font-size: 13px; border: 1px solid var(--border);
  background: var(--surface); color: var(--text-primary);
}
.btn.primary { background: var(--accent); border-color: var(--accent); color: #fff; font-weight: 600; }
.body { display: flex; flex: 1; min-height: 0; }
.sidebar {
  width: 240px; flex: none; background: var(--surface);
  border-right: 1px solid var(--border); padding: 12px 8px; overflow-y: auto;
}
.canvas { flex: 1; min-width: 0; background: var(--canvas-bg); position: relative; }
.detail {
  width: 300px; flex: none; background: var(--surface);
  border-left: 1px solid var(--border); padding: 16px;
  display: flex; flex-direction: column; gap: 16px; overflow-y: auto;
}
.empty {
  flex: 1; display: flex; flex-direction: column; align-items: center;
  justify-content: center; gap: 16px;
}
.mono { font-family: var(--font-mono); }
```

- [ ] **Step 2: TopBar / EmptyState / App 작성**

`src/components/TopBar.tsx`:

```tsx
import { FolderOpen, Plus, RefreshCw } from "lucide-react";
import { useWorkspace } from "../workspace/useWorkspace";
import { openFolder } from "./EmptyState";

export default function TopBar() {
  const root = useWorkspace((s) => s.root);
  const refresh = useWorkspace((s) => s.refresh);
  const addContext = useWorkspace((s) => s.addContext);

  async function onAddContext() {
    const name = window.prompt("컨텍스트 이름 (한글)");
    if (!name) return;
    const fileName = window.prompt("파일명 (예: order.yml)");
    if (!fileName || !fileName.endsWith(".yml")) return;
    await addContext(fileName, name);
  }

  return (
    <header className="topbar">
      <span className="logo">biz-doc studio</span>
      {root && (
        <span className="ws-pill">
          <FolderOpen size={14} /> {root}
        </span>
      )}
      <span className="spacer" />
      <button className="btn" onClick={openFolder}>
        <FolderOpen size={13} /> 폴더 열기
      </button>
      {root && (
        <>
          <button className="btn" onClick={refresh}>
            <RefreshCw size={13} /> 새로고침
          </button>
          <button className="btn primary" onClick={onAddContext}>
            <Plus size={13} /> 컨텍스트 추가
          </button>
        </>
      )}
    </header>
  );
}
```

`src/components/EmptyState.tsx`:

```tsx
import { useEffect, useState } from "react";
import { FolderOpen } from "lucide-react";
import { open } from "@tauri-apps/plugin-dialog";
import { appConfigDir } from "@tauri-apps/api/path";
import { useWorkspace } from "../workspace/useWorkspace";
import { loadRecent, saveRecent } from "../workspace/workspaceService";

export async function openFolder(path?: string): Promise<void> {
  const target = path ?? (await open({ directory: true }));
  if (typeof target !== "string") return;
  const s = useWorkspace.getState();
  await s.openWorkspace(target);
  const cfg = await appConfigDir();
  const recent = await loadRecent(s.fs, cfg);
  await saveRecent(s.fs, cfg, [target, ...recent]);
}

export default function EmptyState() {
  const [recent, setRecent] = useState<string[]>([]);
  const fs = useWorkspace((s) => s.fs);

  useEffect(() => {
    appConfigDir().then((cfg) => loadRecent(fs, cfg)).then(setRecent).catch(() => {});
  }, [fs]);

  return (
    <div className="empty">
      <h2>워크스페이스를 열어주세요</h2>
      <p style={{ color: "var(--text-secondary)" }}>
        context/*.yml 명세가 있는 폴더를 선택하면 Context Map이 표시됩니다.
      </p>
      <button className="btn primary" onClick={() => openFolder()}>
        <FolderOpen size={13} /> 폴더 열기
      </button>
      {recent.length > 0 && (
        <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
          {recent.map((p) => (
            <button key={p} className="btn" onClick={() => openFolder(p)}>
              {p}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

`src/App.tsx`:

```tsx
import "@fontsource/noto-sans-kr/400.css";
import "@fontsource/noto-sans-kr/500.css";
import "@fontsource/noto-sans-kr/700.css";
import "@fontsource/jetbrains-mono/400.css";
import "@fontsource/jetbrains-mono/600.css";
import "./styles/tokens.css";
import "./styles/app.css";
import TopBar from "./components/TopBar";
import EmptyState from "./components/EmptyState";
import { useWorkspace } from "./workspace/useWorkspace";

export default function App() {
  const root = useWorkspace((s) => s.root);
  return (
    <div className="app">
      <TopBar />
      {root ? (
        <div className="body">
          <aside className="sidebar">탐색기 (Task 8)</aside>
          <main className="canvas" />
          <aside className="detail">상세 (Task 12)</aside>
        </div>
      ) : (
        <EmptyState />
      )}
    </div>
  );
}
```

- [ ] **Step 3: 수동 확인**

Run: `npm run tauri dev`
Expected: EmptyState 표시 → "폴더 열기"로 리포의 `document/sample` 선택 → 3단 레이아웃 전환, 상단바에 경로 pill. 앱 재시작 시 최근 목록에 경로 표시. `npm test` 여전히 전체 PASS.

- [ ] **Step 4: Commit**

```bash
git add src/styles src/components/TopBar.tsx src/components/EmptyState.tsx src/App.tsx src/main.tsx
git commit -m "feat: 디자인 토큰, 앱 셸, 워크스페이스 열기"
```

---

### Task 8: 사이드바 탐색기 (모델/서비스 그룹)

**Files:**
- Create: `src/components/Sidebar.tsx`
- Modify: `src/App.tsx` (placeholder 교체), `src/styles/app.css` (트리 스타일 추가)

**Interfaces:**
- Consumes: `useWorkspace` (contexts, selection, select)
- Produces: 컨텍스트 → (모델 그룹 | 서비스 그룹) → 도메인 행 트리. 행 클릭 → `select({kind:"domain",...})`. 유형 아이콘 매핑 `typeIcon(type): LucideIcon` + 유형 색 CSS 클래스 `type-color(type)` — DomainNode(Task 10)와 공유하기 위해 `src/components/typeVisuals.ts`로 분리.

- [ ] **Step 1: typeVisuals 유틸 작성**

`src/components/typeVisuals.ts`:

```ts
import {
  Component, Database, Diamond, List, Workflow, type LucideIcon,
} from "lucide-react";
import type { DomainType } from "../workspace/schema";

export const typeIcon: Record<DomainType, LucideIcon> = {
  "Root Aggregate": Database,
  Entity: Component,
  Value: Diamond,
  Stereotype: List,
  Service: Workflow,
};

export const typeVar: Record<DomainType, string> = {
  "Root Aggregate": "root",
  Entity: "entity",
  Value: "value",
  Stereotype: "stereotype",
  Service: "service",
};
// 사용: color: `var(--type-${typeVar[type]})`, 배경: `var(--type-${typeVar[type]}-soft)`
```

- [ ] **Step 2: Sidebar 작성**

`src/components/Sidebar.tsx`:

```tsx
import { Box, ChevronDown } from "lucide-react";
import { useWorkspace } from "../workspace/useWorkspace";
import { typeIcon, typeVar } from "./typeVisuals";

export default function Sidebar() {
  const contexts = useWorkspace((s) => s.contexts);
  const selection = useWorkspace((s) => s.selection);
  const select = useWorkspace((s) => s.select);

  return (
    <nav className="sidebar">
      <div className="tree-caption">탐색기</div>
      {contexts.map((c) =>
        c.error ? (
          <div key={c.fileName} className="tree-row error" title={c.error}>
            <Box size={14} color="var(--danger)" /> {c.fileName} (파싱 오류)
          </div>
        ) : (
          <ContextTree key={c.fileName} fileName={c.fileName} />
        )
      )}
    </nav>
  );

  function ContextTree({ fileName }: { fileName: string }) {
    const ctx = contexts.find((c) => c.fileName === fileName)!;
    const domains = Object.entries(ctx.spec.domain ?? {});
    const models = domains.filter(([, d]) => d.meta.identity.type !== "Service");
    const services = domains.filter(([, d]) => d.meta.identity.type === "Service");
    return (
      <div>
        <div
          className={`tree-row ${selection?.kind === "context" && selection.fileName === fileName ? "selected" : ""}`}
          onClick={() => select({ kind: "context", fileName })}
        >
          <ChevronDown size={12} color="var(--text-secondary)" />
          <Box size={14} color="var(--accent)" />
          <b>{ctx.spec.info.context.name}</b>
        </div>
        <Group label="모델" entries={models} />
        {services.length > 0 && <Group label="서비스" entries={services} />}
      </div>
    );

    function Group({ label, entries }: { label: string; entries: typeof domains }) {
      return (
        <>
          <div className="tree-caption indent">{label}</div>
          {entries.map(([key, d]) => {
            const Icon = typeIcon[d.meta.identity.type];
            const isSel =
              selection?.kind === "domain" &&
              selection.fileName === fileName &&
              selection.domainKey === key;
            return (
              <div
                key={key}
                className={`tree-row domain ${isSel ? "selected" : ""}`}
                onClick={() => select({ kind: "domain", fileName, domainKey: key })}
              >
                <Icon size={13} color={`var(--type-${typeVar[d.meta.identity.type]})`} />
                {d.meta.name}
              </div>
            );
          })}
        </>
      );
    }
  }
}
```

`src/styles/app.css`에 추가:

```css
.tree-caption {
  font-size: 11px; font-weight: 700; letter-spacing: 0.5px;
  color: var(--text-secondary); padding: 6px 8px 2px;
}
.tree-caption.indent { padding-left: 30px; font-size: 10px; }
.tree-row {
  display: flex; align-items: center; gap: 6px; padding: 6px 8px;
  border-radius: 6px; cursor: pointer; user-select: none;
}
.tree-row.domain { padding-left: 30px; }
.tree-row:hover { background: var(--bg); }
.tree-row.selected { background: var(--accent-soft); color: var(--accent); font-weight: 600; }
.tree-row.error { color: var(--danger); }
```

`src/App.tsx`의 `<aside className="sidebar">…` placeholder를 `<Sidebar />`로 교체 (`.sidebar` 클래스는 Sidebar 내부 nav가 가지므로 aside 래퍼 제거).

- [ ] **Step 3: 수동 확인**

Run: `npm run tauri dev` → sample 워크스페이스 열기
Expected: "스윙 레코더" 아래 모델 4개(세션/레코더/결과/구간) + 서비스 그룹(스윙 평가). 클릭 시 하이라이트.

- [ ] **Step 4: Commit**

```bash
git add src/components/Sidebar.tsx src/components/typeVisuals.ts src/App.tsx src/styles/app.css
git commit -m "feat: 사이드바 탐색기 (모델/서비스 그룹)"
```

---

### Task 9: ContextMap — React Flow 캔버스 + 레이아웃 저장

**Files:**
- Create: `src/components/ContextMap.tsx`, `src/components/nodes/ContextGroupNode.tsx`, `src/components/nodes/DomainNode.tsx`(임시 단순 버전)
- Modify: `src/App.tsx`, `src/styles/app.css`

**Interfaces:**
- Consumes: `useWorkspace`, `LayoutFile`
- Produces:
  - 노드 ID 규약: 컨텍스트 노드 = `ctx:<context-id>`, 도메인 노드 = `dom:<domain-id>` (layout.json 키로도 사용)
  - `buildFlow(contexts, layout): { nodes: Node[]; edges: Edge[] }` — 컨텍스트 그룹 노드(parent) + 도메인 자식 노드(parentId, `extent:"parent"`) + relationships → edges (`id: rel:<fileName>:<index>`, label = 관계명)
  - 기본 배치: 레이아웃에 없는 컨텍스트는 (40, 40)부터 900px 간격 가로 배열, 도메인은 컨텍스트 내부 (40, 80)부터 280px 간격 그리드
  - 드래그 종료 → `saveNodePosition(nodeId, pos)` (즉시 저장; POC에서 debounce 생략)
  - 노드 클릭 → select(domain|context), 엣지 클릭 → select(relationship)

- [ ] **Step 1: buildFlow + ContextMap 작성**

`src/components/ContextMap.tsx`:

```tsx
import { useCallback, useMemo } from "react";
import {
  Background, Controls, ReactFlow, type Edge, type Node,
  type NodeChange, applyNodeChanges,
} from "@xyflow/react";
import "@xyflow/react/dist/style.css";
import { useWorkspace } from "../workspace/useWorkspace";
import type { ContextFile, LayoutFile } from "../workspace/workspaceService";
import ContextGroupNode from "./nodes/ContextGroupNode";
import DomainNode from "./nodes/DomainNode";

const nodeTypes = { context: ContextGroupNode, domain: DomainNode };

export function buildFlow(
  contexts: ContextFile[], layout: LayoutFile
): { nodes: Node[]; edges: Edge[] } {
  const nodes: Node[] = [];
  const edges: Edge[] = [];
  let ctxX = 40;
  for (const c of contexts.filter((c) => !c.error)) {
    const ctxId = `ctx:${c.spec.info.context.id}`;
    const ctxPos = layout.nodes[ctxId] ?? { x: ctxX, y: 40 };
    ctxX += 940;
    nodes.push({
      id: ctxId, type: "context", position: ctxPos,
      data: { fileName: c.fileName, spec: c.spec },
      style: { width: 880, height: 780, zIndex: -1 },
    });
    let i = 0;
    for (const [key, d] of Object.entries(c.spec.domain ?? {})) {
      const domId = `dom:${d.meta.identity.id}`;
      const isService = d.meta.identity.type === "Service";
      const fallback = isService
        ? { x: 36 + (i % 3) * 280, y: 620 }
        : { x: 40 + (i % 3) * 280, y: 80 + Math.floor(i / 3) * 260 };
      nodes.push({
        id: domId, type: "domain", parentId: ctxId, extent: "parent",
        position: layout.nodes[domId] ?? fallback,
        data: { fileName: c.fileName, domainKey: key, domain: d },
      });
      i++;
    }
    (c.spec.relationships ?? []).forEach((r, idx) => {
      edges.push({
        id: `rel:${c.fileName}:${idx}`,
        source: `dom:${r.from["domain-id"]}`,
        target: `dom:${r.to["domain-id"]}`,
        label: r.relationship,
        type: "smoothstep",
      });
    });
  }
  return { nodes, edges };
}

export default function ContextMap() {
  const contexts = useWorkspace((s) => s.contexts);
  const layout = useWorkspace((s) => s.layout);
  const select = useWorkspace((s) => s.select);
  const saveNodePosition = useWorkspace((s) => s.saveNodePosition);

  const { nodes, edges } = useMemo(
    () => buildFlow(contexts, layout),
    [contexts, layout]
  );

  const onNodesChange = useCallback(
    (changes: NodeChange[]) => {
      for (const ch of changes) {
        if (ch.type === "position" && ch.dragging === false && ch.position) {
          void saveNodePosition(ch.id, ch.position);
        }
      }
    },
    [saveNodePosition]
  );

  return (
    <main className="canvas">
      <ReactFlow
        nodes={nodes}
        edges={edges}
        nodeTypes={nodeTypes}
        onNodesChange={onNodesChange}
        onNodeClick={(_, n) => {
          if (n.type === "domain")
            select({ kind: "domain", fileName: n.data.fileName as string, domainKey: n.data.domainKey as string });
          else select({ kind: "context", fileName: n.data.fileName as string });
        }}
        onEdgeClick={(_, e) => {
          const [, fileName, idx] = e.id.split(":");
          select({ kind: "relationship", fileName, index: Number(idx) });
        }}
        fitView
      >
        <Background />
        <Controls />
      </ReactFlow>
    </main>
  );
}
```

주의: React Flow는 controlled `nodes`를 넘기면 드래그 중간 상태를 직접 관리해야 한다. POC에서는 **uncontrolled 패턴**이 단순하다 — 위 코드로 드래그가 되지 않으면 `defaultNodes`/`defaultEdges` + `key={contexts를 요약한 문자열}` 리마운트 방식으로 전환하고, `onNodeDragStop={(_, n) => saveNodePosition(n.id, n.position)}`을 사용한다.

- [ ] **Step 2: 임시 노드 컴포넌트 작성**

`src/components/nodes/ContextGroupNode.tsx` (구역/스타일은 Task 10에서 완성):

```tsx
import type { NodeProps } from "@xyflow/react";
import { Box } from "lucide-react";
import type { ContextSpec } from "../../workspace/schema";

export default function ContextGroupNode({ data }: NodeProps) {
  const spec = data.spec as ContextSpec;
  return (
    <div className="ctx-box">
      <div className="ctx-title">
        <Box size={15} color="var(--accent)" />
        <b>{spec.info.context.name}</b>
        <span className="ctx-file mono">{data.fileName as string}</span>
      </div>
    </div>
  );
}
```

`src/components/nodes/DomainNode.tsx` (임시 — Task 10에서 카드 완성):

```tsx
import { Handle, Position, type NodeProps } from "@xyflow/react";
import type { DomainSpec } from "../../workspace/schema";

export default function DomainNode({ data }: NodeProps) {
  const d = data.domain as DomainSpec;
  return (
    <div className="domain-card">
      <Handle type="target" position={Position.Top} />
      <div style={{ padding: 8 }}>{d.meta.name}</div>
      <Handle type="source" position={Position.Bottom} />
    </div>
  );
}
```

`src/styles/app.css`에 추가:

```css
.ctx-box {
  width: 100%; height: 100%; border: 1px solid var(--accent);
  border-radius: 12px; background: rgba(255, 255, 255, 0.7);
}
.ctx-title { display: flex; align-items: center; gap: 8px; padding: 14px 16px; font-size: 14px; }
.ctx-file {
  font-size: 11px; color: var(--text-secondary);
  background: var(--bg); border-radius: 4px; padding: 2px 8px;
}
.domain-card {
  width: 240px; background: var(--surface); border-radius: 8px;
  border: 1px solid var(--border);
  box-shadow: 0 2px 8px rgba(31, 36, 48, 0.08); font-size: 12px;
}
```

`src/App.tsx`의 `<main className="canvas" />`를 `<ContextMap />`으로 교체.

- [ ] **Step 3: 수동 확인**

Run: `npm run tauri dev` → sample 열기
Expected: 컨텍스트 박스 1개 + 도메인 노드 5개 + 관계 엣지 2개(포함됨/사용함) 렌더. 노드 드래그 후 `document/sample/.config/layout.json` 생성·좌표 기록, 새로고침 시 위치 유지. 노드/엣지 클릭 시 사이드바 하이라이트 연동.

- [ ] **Step 4: Commit**

```bash
git add src/components/ContextMap.tsx src/components/nodes src/App.tsx src/styles/app.css
git commit -m "feat: React Flow 컨텍스트 맵 및 레이아웃 저장"
```

---

### Task 10: 도메인 카드 5종 + 컨텍스트 모델/서비스 구역

**Files:**
- Modify: `src/components/nodes/DomainNode.tsx`, `src/components/nodes/ContextGroupNode.tsx`, `src/styles/app.css`

**Interfaces:**
- Consumes: `typeIcon`, `typeVar` (Task 8), `DomainSpec`
- Produces: ui.md의 카드 디자인 그대로 — 유형 헤더 밴드(아이콘+유형명, Root Aggregate는 솔리드+흰 글씨), 한글 이름+영문 키(모노), 속성 행(이름 모노 + 설명 + 타입 칩), 비즈니스 로직 섹션(bg 배경), Stereotype 값 도트, Service 오퍼레이션 + related-domains 칩. 컨텍스트 박스 하단에 서비스 구역 밴드(청록 저알파 + "서비스" 라벨) + 상단 "모델" 라벨 + 구분선.

- [ ] **Step 1: DomainNode 완성**

`src/components/nodes/DomainNode.tsx` 전체 교체:

```tsx
import { Handle, Position, type NodeProps } from "@xyflow/react";
import type { DomainSpec } from "../../workspace/schema";
import { typeIcon, typeVar } from "../typeVisuals";

export default function DomainNode({ data, selected }: NodeProps) {
  const d = data.domain as DomainSpec;
  const key = data.domainKey as string;
  const t = d.meta.identity.type;
  const v = typeVar[t];
  const Icon = typeIcon[t];
  const isRoot = t === "Root Aggregate";

  return (
    <div
      className="domain-card"
      style={{
        borderColor: `var(--type-${v})`,
        borderWidth: selected || isRoot ? 1.5 : 1,
      }}
    >
      <Handle type="target" position={Position.Top} />
      <div
        className="card-band"
        style={
          isRoot
            ? { background: "var(--accent)", color: "#fff" }
            : { background: `var(--type-${v}-soft)`, color: `var(--type-${v})` }
        }
      >
        <Icon size={12} />
        <span>{t}</span>
      </div>
      <div className="card-head">
        <div className="card-title">{d.meta.name}</div>
        <div className="card-sub mono">{key}</div>
      </div>
      {t === "Stereotype" ? (
        <div className="card-rows">
          {(d.values ?? []).map((x) => (
            <div className="attr-row" key={x.name}>
              <span className="value-name mono">
                <i className="dot" style={{ background: `var(--type-${v})` }} />
                {x.name}
              </span>
              <span className="desc">{x.description}</span>
            </div>
          ))}
        </div>
      ) : t === "Service" ? (
        <div className="card-rows">
          {(d.operations ?? []).map((op) => (
            <div className="op-row" key={op.name}>
              <span className="mono op-name">{op.name}</span>
              <span className="desc">{op.description}</span>
              {op["related-domains"] && (
                <span className="chips">
                  {op["related-domains"].map((rd) => (
                    <span className="chip mono" key={rd}>{rd}</span>
                  ))}
                </span>
              )}
            </div>
          ))}
        </div>
      ) : (
        <>
          <div className="card-rows">
            {(d.attributes ?? []).map((a) => (
              <div className="attr-row" key={a.name}>
                <span>
                  <span className="mono attr-name">{a.name}</span>
                  <span className="desc block">{a.description}</span>
                </span>
                <span className="chip mono">{a.type}</span>
              </div>
            ))}
          </div>
          {(d["business-logic"] ?? []).length > 0 && (
            <div className="rule-section">
              <div className="rule-label">비즈니스 로직</div>
              {d["business-logic"]!.map((r) => (
                <div key={r.name}>
                  <div className="mono rule-name">{r.name}</div>
                  <div className="desc">{r.description}</div>
                </div>
              ))}
            </div>
          )}
        </>
      )}
      <Handle type="source" position={Position.Bottom} />
    </div>
  );
}
```

- [ ] **Step 2: ContextGroupNode 구역 완성**

`src/components/nodes/ContextGroupNode.tsx` 전체 교체:

```tsx
import type { NodeProps } from "@xyflow/react";
import { Box } from "lucide-react";
import type { ContextSpec } from "../../workspace/schema";

const SERVICE_BAND = 200; // 하단 서비스 구역 높이(px)

export default function ContextGroupNode({ data }: NodeProps) {
  const spec = data.spec as ContextSpec;
  return (
    <div className="ctx-box">
      <div className="ctx-title">
        <Box size={15} color="var(--accent)" />
        <b>{spec.info.context.name}</b>
        <span className="ctx-file mono">{data.fileName as string}</span>
      </div>
      <div className="zone-label" style={{ left: 18, top: 48 }}>모델</div>
      <div className="service-zone" style={{ height: SERVICE_BAND }}>
        <div className="zone-label" style={{ color: "var(--type-service)" }}>서비스</div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 카드/구역 CSS 추가**

`src/styles/app.css`에 추가:

```css
.card-band {
  display: flex; align-items: center; gap: 6px; padding: 5px 12px;
  border-radius: 7px 7px 0 0; font-size: 11px; font-weight: 700; letter-spacing: 0.5px;
}
.card-head { padding: 10px 12px 8px; border-bottom: 1px solid var(--border); }
.card-title { font-size: 13px; font-weight: 700; }
.card-sub { font-size: 10.5px; color: var(--text-secondary); }
.card-rows .attr-row {
  display: flex; justify-content: space-between; align-items: center; padding: 6px 12px;
}
.attr-name { font-size: 12px; font-weight: 500; }
.desc { font-size: 10px; color: var(--text-secondary); }
.desc.block { display: block; }
.chip {
  background: var(--bg); border-radius: 4px; padding: 2px 6px;
  font-size: 10px; color: var(--text-secondary);
}
.chips { display: flex; gap: 4px; margin-top: 4px; }
.value-name { display: flex; align-items: center; gap: 6px; font-size: 12px; font-weight: 600; }
.dot { width: 6px; height: 6px; border-radius: 50%; display: inline-block; }
.op-row { display: flex; flex-direction: column; gap: 2px; padding: 8px 12px; }
.op-name { font-size: 11px; font-weight: 600; }
.rule-section {
  background: var(--bg); border-top: 1px solid var(--border);
  border-radius: 0 0 7px 7px; padding: 6px 12px 8px;
  display: flex; flex-direction: column; gap: 4px;
}
.rule-label {
  font-size: 10px; font-weight: 700; letter-spacing: 0.4px; color: var(--text-secondary);
}
.rule-name { font-size: 11px; font-weight: 600; }
.zone-label {
  position: absolute; font-size: 10px; font-weight: 700;
  letter-spacing: 0.5px; color: var(--text-secondary);
}
.service-zone {
  position: absolute; left: 16px; right: 16px; bottom: 12px;
  background: rgba(8, 145, 178, 0.05); border-radius: 10px;
  border-top: 1px solid var(--border);
}
.service-zone .zone-label { left: 12px; top: 10px; }
```

- [ ] **Step 4: 수동 확인**

Run: `npm run tauri dev` → sample 열기
Expected: 목업(page.pen)과 동일한 카드 5종 — Root Aggregate 솔리드 밴드, 타입 칩, Stereotype 도트, Service 카드가 하단 청록 구역 안. Task 9에서 서비스 노드 fallback y=620이 구역 내부에 위치.

- [ ] **Step 5: Commit**

```bash
git add src/components/nodes src/styles/app.css
git commit -m "feat: 도메인 카드 5종 및 컨텍스트 모델/서비스 구역"
```

---

### Task 11: 관계 편집 — 연결 생성 / 이름 수정 / 삭제

**Files:**
- Modify: `src/components/ContextMap.tsx`
- Create: `src/components/panels/RelationshipPanel.tsx`

**Interfaces:**
- Consumes: `addRelationship`, `updateRelationship`, `removeRelationship`, `Selection`
- Produces: 핸들 드래그 연결(`onConnect`) → `window.prompt("관계명")` → 같은 컨텍스트끼리는 해당 파일에, 다른 컨텍스트 노드 간에는 **source 컨텍스트 파일**에 관계 추가. `RelationshipPanel({fileName, index})` — 관계명 input(blur 시 저장) + 삭제 버튼. DetailPanel(Task 12)에서 selection.kind === "relationship"일 때 렌더.

- [ ] **Step 1: onConnect 구현**

`ContextMap.tsx`의 ReactFlow에 prop 추가:

```tsx
const contextsRef = contexts; // 클로저용
const addRelationship = useWorkspace((s) => s.addRelationship);

function findDomain(nodeId: string) {
  const domainId = nodeId.replace(/^dom:/, "");
  for (const c of contextsRef.filter((c) => !c.error)) {
    for (const d of Object.values(c.spec.domain ?? {})) {
      if (d.meta.identity.id === domainId)
        return { fileName: c.fileName, contextId: c.spec.info.context.id, domainId };
    }
  }
  return null;
}

const onConnect = useCallback(
  (conn: Connection) => {
    if (!conn.source || !conn.target) return;
    const from = findDomain(conn.source);
    const to = findDomain(conn.target);
    if (!from || !to) return;
    const label = window.prompt("관계명 (예: 포함됨)");
    if (!label) return;
    void addRelationship(from.fileName, {
      from: { "context-id": from.contextId, "domain-id": from.domainId },
      to: { "context-id": to.contextId, "domain-id": to.domainId },
      relationship: label,
    });
  },
  [contexts, addRelationship]
);
// <ReactFlow ... onConnect={onConnect}>
```

(`Connection` 타입은 `@xyflow/react`에서 import.)

- [ ] **Step 2: RelationshipPanel 작성**

`src/components/panels/RelationshipPanel.tsx`:

```tsx
import { Trash2 } from "lucide-react";
import { useWorkspace } from "../../workspace/useWorkspace";

export default function RelationshipPanel({
  fileName, index,
}: { fileName: string; index: number }) {
  const contexts = useWorkspace((s) => s.contexts);
  const updateRelationship = useWorkspace((s) => s.updateRelationship);
  const removeRelationship = useWorkspace((s) => s.removeRelationship);
  const select = useWorkspace((s) => s.select);

  const ctx = contexts.find((c) => c.fileName === fileName);
  const rel = ctx?.spec.relationships?.[index];
  if (!ctx || !rel) return null;

  const nameOf = (domainId: string) =>
    Object.values(ctx.spec.domain ?? {}).find((d) => d.meta.identity.id === domainId)
      ?.meta.name ?? domainId;

  return (
    <div className="panel-section">
      <h3>관계</h3>
      <div className="desc">
        {nameOf(rel.from["domain-id"])} → {nameOf(rel.to["domain-id"])}
      </div>
      <label>
        관계명
        <input
          defaultValue={rel.relationship}
          onBlur={(e) => {
            if (e.target.value && e.target.value !== rel.relationship)
              void updateRelationship(fileName, index, e.target.value);
          }}
        />
      </label>
      <button
        className="btn danger"
        onClick={async () => {
          await removeRelationship(fileName, index);
          select(null);
        }}
      >
        <Trash2 size={13} /> 관계 삭제
      </button>
    </div>
  );
}
```

- [ ] **Step 3: 수동 확인**

Run: `npm run tauri dev`
Expected: 카드 하단 핸들을 다른 카드로 드래그 → 관계명 입력 → 엣지 생김 + yml `relationships`에 추가(파일 확인). 엣지 클릭 → (Task 12 전이므로 콘솔로 selection 확인 가능) — Task 12 이후 패널에서 이름 수정/삭제 동작.

- [ ] **Step 4: Commit**

```bash
git add src/components/ContextMap.tsx src/components/panels/RelationshipPanel.tsx
git commit -m "feat: 관계 연결 생성 및 편집"
```

---

### Task 12: 상세 편집 패널 (DetailPanel)

**Files:**
- Create: `src/components/panels/DetailPanel.tsx`
- Modify: `src/App.tsx` (placeholder 교체), `src/styles/app.css`

**Interfaces:**
- Consumes: `useWorkspace` 전 액션, `RelationshipPanel` (Task 11), `typeVar`
- Produces: selection에 따라 3가지 모드 렌더. 도메인 모드 구성(ui.md 순서): 유형 배지+이름+영문 키 → 설명 → 속성(묶음 카드: 상단 속성명+타입 chip 우측 정렬, 아래 설명 / 추가·삭제) → 비즈니스 로직 or values or operations (유형별) → 관계 목록 → **이력(작성자/생성/수정, 최하단 읽기 전용)** → 도메인 삭제. 모든 input은 blur 시 저장(즉시 파일 반영). ID는 표시하지 않음.

- [ ] **Step 1: DetailPanel 작성**

`src/components/panels/DetailPanel.tsx`:

```tsx
import { Plus, Trash2 } from "lucide-react";
import { useWorkspace } from "../../workspace/useWorkspace";
import type { DomainSpec } from "../../workspace/schema";
import { typeVar } from "../typeVisuals";
import RelationshipPanel from "./RelationshipPanel";

export default function DetailPanel() {
  const selection = useWorkspace((s) => s.selection);
  if (!selection) return <aside className="detail empty-detail">선택된 항목 없음</aside>;
  return (
    <aside className="detail">
      {selection.kind === "domain" && <DomainPanel {...selection} />}
      {selection.kind === "relationship" && (
        <RelationshipPanel fileName={selection.fileName} index={selection.index} />
      )}
      {selection.kind === "context" && <ContextPanel fileName={selection.fileName} />}
    </aside>
  );
}

function DomainPanel({ fileName, domainKey }: { fileName: string; domainKey: string }) {
  const s = useWorkspace();
  const ctx = s.contexts.find((c) => c.fileName === fileName);
  const d: DomainSpec | undefined = ctx?.spec.domain?.[domainKey];
  if (!ctx || !d) return null;
  const t = d.meta.identity.type;
  const v = typeVar[t];
  const myRels = (ctx.spec.relationships ?? [])
    .map((r, i) => ({ r, i }))
    .filter(({ r }) =>
      [r.from["domain-id"], r.to["domain-id"]].includes(d.meta.identity.id)
    );

  return (
    <>
      <div className="panel-section">
        <div className="panel-title-row">
          <input
            className="panel-title"
            defaultValue={d.meta.name}
            key={`${domainKey}-name-${d.meta.name}`}
            onBlur={(e) =>
              e.target.value !== d.meta.name &&
              void s.updateDomainMeta(fileName, domainKey, { name: e.target.value })
            }
          />
          <span className="chip" style={{ background: `var(--type-${v}-soft)`, color: `var(--type-${v})` }}>
            {t}
          </span>
        </div>
        <div className="mono desc">{domainKey}</div>
        <textarea
          defaultValue={d.meta.description ?? ""}
          key={`${domainKey}-desc-${d.meta.description}`}
          rows={2}
          onBlur={(e) =>
            e.target.value !== (d.meta.description ?? "") &&
            void s.updateDomainMeta(fileName, domainKey, { description: e.target.value })
          }
        />
      </div>

      {t !== "Stereotype" && t !== "Service" && (
        <ListSection
          title="속성" items={d.attributes ?? []}
          render={(a, i) => (
            <div className="attr-edit" key={i}>
              <div className="attr-edit-top">
                <input
                  className="mono" defaultValue={a.name} placeholder="name"
                  onBlur={(e) => void s.upsertAttribute(fileName, domainKey, i, { ...a, name: e.target.value })}
                />
                <input
                  className="mono chip-input" defaultValue={a.type} placeholder="type"
                  onBlur={(e) => void s.upsertAttribute(fileName, domainKey, i, { ...a, type: e.target.value })}
                />
                <button className="icon-btn" onClick={() => void s.removeAttribute(fileName, domainKey, i)}>
                  <Trash2 size={12} />
                </button>
              </div>
              <input
                defaultValue={a.description ?? ""} placeholder="설명"
                onBlur={(e) => void s.upsertAttribute(fileName, domainKey, i, { ...a, description: e.target.value })}
              />
            </div>
          )}
          onAdd={() =>
            void s.upsertAttribute(fileName, domainKey, null, { name: "newAttr", type: "String", description: "" })
          }
          addLabel="속성 추가"
        />
      )}

      {t === "Stereotype" && (
        <ListSection
          title="열거 값" items={d.values ?? []}
          render={(x, i) => (
            <SimpleNamedEditor key={i} item={x}
              onSave={(item) => void s.upsertValue(fileName, domainKey, i, item)}
              onRemove={() => void s.removeValue(fileName, domainKey, i)} />
          )}
          onAdd={() => void s.upsertValue(fileName, domainKey, null, { name: "NEW", description: "" })}
          addLabel="값 추가"
        />
      )}

      {t === "Service" && (
        <ListSection
          title="오퍼레이션" items={d.operations ?? []}
          render={(x, i) => (
            <SimpleNamedEditor key={i} item={x}
              onSave={(item) => void s.upsertOperation(fileName, domainKey, i, { ...x, ...item })}
              onRemove={() => void s.removeOperation(fileName, domainKey, i)} />
          )}
          onAdd={() => void s.upsertOperation(fileName, domainKey, null, { name: "newOp", description: "" })}
          addLabel="오퍼레이션 추가"
        />
      )}

      {t !== "Stereotype" && t !== "Service" && (
        <ListSection
          title="비즈니스 로직" items={d["business-logic"] ?? []}
          render={(x, i) => (
            <SimpleNamedEditor key={i} item={x}
              onSave={(item) => void s.upsertLogic(fileName, domainKey, i, item)}
              onRemove={() => void s.removeLogic(fileName, domainKey, i)} />
          )}
          onAdd={() => void s.upsertLogic(fileName, domainKey, null, { name: "newRule", description: "" })}
          addLabel="로직 추가"
        />
      )}

      <div className="panel-section">
        <h3>관계 <span className="desc">{myRels.length}개</span></h3>
        {myRels.map(({ r, i }) => (
          <button key={i} className="btn rel-item"
            onClick={() => s.select({ kind: "relationship", fileName, index: i })}>
            <span className="chip">{r.relationship}</span> →{" "}
            {Object.values(ctx.spec.domain ?? {}).find(
              (x) => x.meta.identity.id ===
                (r.from["domain-id"] === d.meta.identity.id ? r.to["domain-id"] : r.from["domain-id"])
            )?.meta.name ?? "(외부)"}
          </button>
        ))}
      </div>

      <div className="spacer-v" />

      <div className="panel-section meta">
        <div className="rule-label">이력</div>
        <MetaRow k="작성자" v={d.meta.audit.author} />
        <MetaRow k="생성" v={d.meta.audit["created-at"]} />
        <MetaRow k="수정" v={d.meta.audit["updated-at"]} />
      </div>

      <button className="btn danger" onClick={async () => {
        if (!window.confirm(`도메인 "${d.meta.name}"을(를) 삭제할까요?`)) return;
        await s.deleteDomain(fileName, domainKey);
        s.select(null);
      }}>
        <Trash2 size={13} /> 도메인 삭제
      </button>
    </>
  );
}

function ContextPanel({ fileName }: { fileName: string }) {
  const s = useWorkspace();
  const ctx = s.contexts.find((c) => c.fileName === fileName);
  if (!ctx) return null;
  return (
    <div className="panel-section">
      <h3>컨텍스트</h3>
      <div className="panel-title">{ctx.spec.info.context.name}</div>
      <div className="mono desc">{fileName}</div>
      <AddDomainForm fileName={fileName} />
      <button className="btn danger" onClick={async () => {
        if (!window.confirm(`컨텍스트 파일 ${fileName}을(를) 삭제할까요?`)) return;
        await s.deleteContext(fileName);
        s.select(null);
      }}>
        <Trash2 size={13} /> 컨텍스트 삭제
      </button>
    </div>
  );
}

function AddDomainForm({ fileName }: { fileName: string }) {
  const s = useWorkspace();
  return (
    <button className="btn" onClick={() => {
      const key = window.prompt("도메인 키 (영문, 예: Order)");
      if (!key) return;
      const name = window.prompt("한글 이름");
      if (!name) return;
      const type = window.prompt(
        "유형: Root Aggregate | Entity | Value | Stereotype | Service", "Entity"
      );
      if (!type) return;
      void s.addDomain(fileName, key, type as never, name);
    }}>
      <Plus size={13} /> 도메인 추가
    </button>
  );
}

function ListSection<T>({ title, items, render, onAdd, addLabel }: {
  title: string; items: T[]; render: (item: T, i: number) => React.ReactNode;
  onAdd: () => void; addLabel: string;
}) {
  return (
    <div className="panel-section">
      <h3>{title} <span className="desc">{items.length}개</span></h3>
      {items.map(render)}
      <button className="btn soft" onClick={onAdd}>
        <Plus size={12} /> {addLabel}
      </button>
    </div>
  );
}

function SimpleNamedEditor({ item, onSave, onRemove }: {
  item: { name: string; description?: string };
  onSave: (x: { name: string; description?: string }) => void;
  onRemove: () => void;
}) {
  return (
    <div className="attr-edit">
      <div className="attr-edit-top">
        <input className="mono" defaultValue={item.name}
          onBlur={(e) => onSave({ ...item, name: e.target.value })} />
        <button className="icon-btn" onClick={onRemove}><Trash2 size={12} /></button>
      </div>
      <input defaultValue={item.description ?? ""} placeholder="설명"
        onBlur={(e) => onSave({ ...item, description: e.target.value })} />
    </div>
  );
}

function MetaRow({ k, v }: { k: string; v: string }) {
  return (
    <div className="meta-row">
      <span className="desc">{k}</span>
      <span className="desc">{v}</span>
    </div>
  );
}
```

- [ ] **Step 2: 패널 CSS 추가**

`src/styles/app.css`에 추가:

```css
.panel-section { display: flex; flex-direction: column; gap: 8px; }
.panel-section h3 { font-size: 12px; display: flex; justify-content: space-between; }
.panel-title-row { display: flex; align-items: center; gap: 8px; }
.panel-title { font-size: 16px; font-weight: 700; border: none; background: none; width: 100%; }
.panel-section input, .panel-section textarea {
  border: 1px solid var(--border); border-radius: 6px; padding: 6px 8px; width: 100%;
}
.attr-edit {
  border: 1px solid var(--border); border-radius: 8px; padding: 10px 12px;
  display: flex; flex-direction: column; gap: 4px;
}
.attr-edit-top { display: flex; gap: 6px; align-items: center; }
.attr-edit-top input:first-child { font-weight: 600; }
.chip-input { max-width: 110px; background: var(--bg); }
.icon-btn { border: none; background: none; color: var(--text-secondary); }
.btn.soft { background: var(--accent-soft); border-color: transparent; color: var(--accent); justify-content: center; }
.btn.danger { border-color: #f1c3c3; color: var(--danger); justify-content: center; }
.btn.rel-item { justify-content: flex-start; }
.spacer-v { flex: 1; }
.meta { background: var(--bg); border-radius: 8px; padding: 10px 12px; }
.meta-row { display: flex; justify-content: space-between; }
.empty-detail { color: var(--text-secondary); align-items: center; justify-content: center; }
```

`src/App.tsx`의 `<aside className="detail">…` placeholder를 `<DetailPanel />`로 교체.

- [ ] **Step 3: 수동 확인 (핵심 시나리오)**

Run: `npm run tauri dev` → sample 열기. 확인:
1. 스윙 세션 클릭 → 이름/설명/속성 2개/로직 1개/관계/이력 표시, ID 없음
2. 속성 설명 수정 → blur → `swing-session.yml`에 즉시 반영 + `updated-at` 갱신 + 캔버스 카드 갱신
3. 스윙 결과(Stereotype) → 열거 값 편집, 스윙 평가(Service) → 오퍼레이션 편집
4. 도메인 추가(Entity) → 캔버스/사이드바/yml 반영. 도메인 삭제 → 관련 관계도 제거
5. `git diff document/sample` — 수정한 값 외의 줄이 변하지 않았는지(키 순서 보존) 확인

- [ ] **Step 4: Commit**

```bash
git add src/components/panels/DetailPanel.tsx src/App.tsx src/styles/app.css
git commit -m "feat: 상세 편집 패널 (도메인/컨텍스트/관계)"
```

---

### Task 13: 마무리 — 통합 시나리오 검증 + 문서 갱신

**Files:**
- Modify: `CLAUDE.md` (진행 상태), `document/sample/.config/layout.json` (생성되어 있으면 커밋)

**Interfaces:**
- Consumes: 전체
- Produces: v1 완료 상태. 요구사항 대비 수동 체크리스트 통과.

- [ ] **Step 1: 요구사항 체크리스트 검증**

Run: `npm run tauri dev` + `npm test` + `npm run build`
requirements.md의 v1 기능 범위를 순서대로 수동 확인:
- [ ] 컨텍스트: 추가(새 yml 생성) / 삭제(파일 삭제) — TopBar·ContextPanel
- [ ] 도메인: 생성(유형 선택)/수정/삭제, 유형 5종 카드 렌더
- [ ] 속성/비즈니스 로직/열거 값/오퍼레이션 CRUD
- [ ] 관계: 드래그 연결 생성 / 이름 수정 / 삭제
- [ ] 드래그 배치 → layout.json 저장/복원, 줌 컨트롤
- [ ] 외부에서 yml 수정 → 새로고침 반영
- [ ] `git diff`로 키 순서 보존 확인
- [ ] audit 자동 갱신, UI에서 ID 미노출·audit 읽기 전용

발견된 버그는 이 태스크 안에서 수정하고 개별 커밋.

- [ ] **Step 2: CLAUDE.md 진행 상태 갱신**

"진행 상태" 섹션을 다음으로 교체:

```markdown
## 진행 상태 (갱신일 기입)

POC(v1) 구현 완료 — 리포 루트가 Tauri 앱. `npm run tauri dev`로 실행, 개발용 워크스페이스는 `document/sample`.
테스트: `npm test` (vitest). 구현 계획: `docs/superpowers/plans/2026-07-24-poc-v1.md`
```

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md document/sample/.config
git commit -m "docs: POC v1 완료 상태 반영"
```

---

## Self-Review 결과

- **Spec coverage**: requirements.md v1 범위 전 항목이 Task 6(액션)·11(관계)·12(패널)·13(체크리스트)에 매핑됨. 빈 상태(폴더 열기+최근 목록)는 Task 7, 다이어그램·layout.json은 Task 9, 유형별 카드는 Task 10.
- **의도적 POC 단순화** (v1 스펙 위반 아님): 컨텍스트 추가/도메인 추가 입력은 `window.prompt` 사용(전용 다이얼로그는 v2), Service 오퍼레이션의 related-domains 편집은 카드 표시만 지원(패널 편집은 v2).
- **Type consistency**: 노드 ID 규약(`ctx:`/`dom:`/`rel:<file>:<idx>`)은 Task 9 정의를 11·12가 사용. 스토어 시그니처는 Task 6 정의를 7·8·11·12가 사용. `typeVar`/`typeIcon`은 Task 8 정의를 10·12가 사용.
- **위험 지점 명시**: Task 4 round-trip 바이트 동일성(실패 시 대응 지침 포함), Task 6 yaml setIn 시퀀스 자동 생성(보강 지침 포함), Task 9 React Flow controlled/uncontrolled(전환 지침 포함).
